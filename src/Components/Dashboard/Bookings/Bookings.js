import React from 'react';
import Sidebar from '../../Shared/Sidebar/Sidebar';

const Bookings = () => {
    return (
        <div>
            <Sidebar/>
        </div>
    );
};

export default Bookings;